# 🚀 FlightDeals 快速参考

## 角色系统一句话总结

**所有用户通过钱包登录，角色由智能合约或后端系统自动分配，不能手动切换（演示模式除外）。**

---

## 三种角色

| 角色 | 如何获得 | 权限 |
|------|---------|------|
| 👤 **普通用户** | 连接钱包即可 | 浏览市场、购买机票、查看订单 |
| ✈️ **航空公司** | 申请 + 管理员审批 | 发布航班、管理票价、查看统计 |
| 👨‍💼 **管理员** | 合约部署者 | 所有权限 + 审批航司 + 系统管理 |

---

## 登录流程对比

### 传统 Web2 ❌
```
输入邮箱 → 输入密码 → 登录
```

### Web3 ✅
```
连接钱包 → 签名验证 → 自动识别角色 → 登录
```

---

## 当前实现

### 演示模式 (开发测试)
- 🎭 可以手动切换角色
- 🔓 用于演示不同界面
- ⚠️ 顶部有黄色提示条

### 生产模式 (真实环境)
- 🔒 角色自动分配
- 🛡️ 不能手动切换
- ✅ 蓝色提示条

---

## 文件结构

```
src/
├── utils/
│   ├── roleManager.js      # 角色查询和管理
│   └── helpers.js          # 辅助函数
├── components/
│   ├── Navbar.js           # 导航栏 (显示角色)
│   └── ...
└── app/
    └── page.js             # 主应用 (角色逻辑)
```

---

## 关键函数

```javascript
// 查询用户角色
const role = await getUserRole(address);

// 检查页面访问权限
const canAccess = canAccessPage(role, 'dashboard');

// 注册航司 (仅管理员)
await registerAirline(adminAddress, airlineAddress);
```

---

## 快速测试

1. 启动应用: `npm run dev`
2. 连接钱包 (模拟)
3. 点击 "启用演示模式"
4. 切换角色测试不同界面
5. 关闭演示模式查看生产模式

---

## 详细文档

- 📖 `ROLE_SYSTEM_SUMMARY.md` - 角色系统总结
- 📖 `AUTHENTICATION_DESIGN.md` - 完整设计方案
- 📖 `UPDATES.md` - UI/UX 更新日志
- 📖 `QUICKSTART.md` - 快速启动指南

---

## 常见问题

**Q: 为什么不能切换角色？**
A: 生产模式下角色由系统分配，确保安全。启用演示模式可以测试。

**Q: 如何成为航司？**
A: 提交申请 → 管理员审批 → 获得权限。

**Q: 管理员是谁？**
A: 智能合约部署者或多签钱包持有者。

**Q: 演示模式安全吗？**
A: 仅用于开发测试，生产环境必须关闭。

---

祝开发顺利！✈️
